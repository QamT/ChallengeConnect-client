import React from 'react';

export default (props) => (
  <div>
    <form>
      <input type='text' name='user' aria-label='search users' />
      <button type='submit'>Go</button>
    </form>
  </div>
)